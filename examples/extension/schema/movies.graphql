type Movie {
    name: String
    director: String
    book: Book
}

extend type Book {
    movies: [Movie] @computed
}

extend type Query {
    movies: [Movie]
}

input MovieInput {
    name: String
    director: String
}

extend type Mutation {
    createMovie(input: MovieInput): Movie
}